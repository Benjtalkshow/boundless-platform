import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Full blog article page. Plain scaffold pending design. */
export function ArticleDetail() {
  return (
    <Section>
      <Placeholder label='Article Detail' className='min-h-80' />
    </Section>
  );
}
