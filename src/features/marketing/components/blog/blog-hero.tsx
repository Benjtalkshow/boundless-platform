import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Blog index hero. Plain scaffold pending design. */
export function BlogHero() {
  return (
    <Section>
      <Placeholder label='Blog Hero' className='min-h-80' />
    </Section>
  );
}
