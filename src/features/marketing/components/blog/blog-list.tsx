import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Blog index list with filters and pagination. Plain scaffold pending design. */
export function BlogList() {
  return (
    <Section>
      <Placeholder label='Blog List' />
    </Section>
  );
}
