import { Placeholder } from './placeholder';
import { Section } from './section';

/** Homepage hero. Plain scaffold pending design. */
export function HeroSection() {
  return (
    <Section>
      <Placeholder label='Hero' className='min-h-80' />
    </Section>
  );
}
