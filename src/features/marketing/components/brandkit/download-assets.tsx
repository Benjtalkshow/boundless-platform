import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Brand asset download band. Plain scaffold pending design. */
export function DownloadAssets() {
  return (
    <Section>
      <Placeholder label='Download Assets' />
    </Section>
  );
}
