import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Brand color palette. Plain scaffold pending design. */
export function ColorPalette() {
  return (
    <Section>
      <Placeholder label='Color Palette' />
    </Section>
  );
}
