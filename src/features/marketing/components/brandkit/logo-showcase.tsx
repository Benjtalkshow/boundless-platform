import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Logo lockups showcase. Plain scaffold pending design. */
export function LogoShowcase() {
  return (
    <Section>
      <Placeholder label='Logo Showcase' />
    </Section>
  );
}
