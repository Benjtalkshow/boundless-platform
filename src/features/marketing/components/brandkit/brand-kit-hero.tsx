import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Brand kit hero. Plain scaffold pending design. */
export function BrandKitHero() {
  return (
    <Section>
      <Placeholder label='Brand Kit Hero' className='min-h-80' />
    </Section>
  );
}
