import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** About page hero. Plain scaffold pending design. */
export function AboutHero() {
  return (
    <Section>
      <Placeholder label='About Hero' className='min-h-80' />
    </Section>
  );
}
