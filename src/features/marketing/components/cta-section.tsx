import { Placeholder } from './placeholder';
import { Section } from './section';

/** Closing call to action. Plain scaffold pending design. */
export function CtaSection() {
  return (
    <Section>
      <Placeholder label='CTA' />
    </Section>
  );
}
