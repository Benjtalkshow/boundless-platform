import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Careers hero. Plain scaffold pending design. */
export function CareersHero() {
  return (
    <Section>
      <Placeholder label='Careers Hero' className='min-h-80' />
    </Section>
  );
}
