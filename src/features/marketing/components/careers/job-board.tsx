import { Placeholder } from '../placeholder';
import { Section } from '../section';

/** Careers job board with filters. Plain scaffold pending design. */
export function JobBoard() {
  return (
    <Section>
      <Placeholder label='Job Board' />
    </Section>
  );
}
